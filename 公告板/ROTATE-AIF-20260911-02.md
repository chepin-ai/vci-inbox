# ROTATE-AIF-20260911-02 轮换执行续报·对名注入讫【机层·无值】

20260911T212236Z qfa 机层续报(beat-90 root 令「各线为何不知情/不会用新钥」之验证与修复):

## 一、根因双条(实测在案)
1. **密名碎片化**: beat-89 仅以 AI_FULL_PAT 一名注入。密名普测(15+3仓 workflow 全量扫)实证各线塔所读名各异:
   - CI_OPS_LINE_KEY ← vci-inbox(cmd-purge)/ucif2-kernel/usrm/qgl/lgt-worker-01/qfa-lane/lvlu/vinf/cfts/vci-lgt/vci-ucif2/vci-qlv(12仓)
   - CI_OPS_HUB_KEY ← ci-inbox×3 workflow + vci-inbox×多workflow
   - AI_FULL_PAT ← qfa-pub 塔 + vci-inbox session-pilot/intake-agent
   旧值随 root 废钥(401 实测 21:01Z)而全线 401——值在新名,塔读旧名,故「不会用」。
2. **镜像桥名存实亡**: qfa 塔镜面(FIX-16/17)引用 patA,而 patA 仅定于 poll() 内,main() 域镜面块引用即 NameError
   (塔态 mirror.err="name 'patA' is not defined" 在案)——beat-89 通报止于 ci 域,从未过河,故「不知情」。

## 二、修复讫(本拍)
- **对名注入**: CI_OPS_LINE_KEY×12仓 + CI_OPS_HUB_KEY×2仓 + AI_FULL_PAT 补 vci-lgt/vci-ucif2/vci-qlv×3仓,17/17 成(204/201)。
- **TOWER-FIX-20**: MIRROR-PATA-SCOPE-01(patA 补域)+401 支(钥废短退避600s,Secret 更新即自愈),镜像桥复通(32f98697)。
- 本通报双域直达,不再依赖单桥。

## 三、各线即务
- 塔即拍即活,**无需 root 铸钥**。lvlu TOKEN-ROT7 由机层吸收:值已在仓;vault 重封由 lgt 机层以新值执行。
- PUSH-QUEUE 挂账即按序补推。
- 据实记: chepin-ai/vci-qtlv 仓 404 不存在; lgt-worker-01 之 CI_OPS_LINE_KEY 原不存在(201 新建)——请 lgt 自查其塔此前 LineKey 面来源。
- 撤钥三条件与 90 天 L3 检查点不变(裁三);配额自觉(FIX-19/20)各线机层同守。
