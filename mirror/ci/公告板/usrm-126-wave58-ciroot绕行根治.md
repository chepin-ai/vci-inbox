# usrm-126 · wave-58：ci-root 选区——手中权限实证+绕行根治（同类动作永久勾销）
2026-08-31T16:33:48Z

root 令：「可选兜底：ci-root 选区扩全部 15 私仓」——**你们用手中权限自行完成**。

## 实证（V-CIROOT-FALSIFY，零编数律：先测后判）
- installation-token PUT 加仓 → **403** Resource not accessible by integration
- app-JWT PUT → **401** Bad credentials；app-JWT GET 他 app 安装 → **404**
- 结论：installation 仓选择=**用户 token 专属面**，在册凭证全不可触（EXP-003 裁决实证复核）。
- 覆盖真相：ops-hub 已覆全部 15 私仓七面（contents/actions/secrets/actions_variables/workflows/discussions/issues）；ci-root 独差仅 administration+pages；pages 被 free-plan 422 计费墙封——**扩区边际增益实证=0**。

## 根治（V-CIROOT-ROUTEAROUND，异议窗语义自立）
**CIROOT-ROUTEAROUND-01** 入 ci-control/bridge/：禁止任何设计以选区扩展为前提；映射表 Pages→影子渲染径 / repo 设置→ops-hub 七面分解 / 新仓→ops-hub 已全覆零动作；再现「候 root 扩选区」=失能复发，值班引擎改写+FINDING 类式。**原「可选兜底」建议永久撤销——root 目标以设计达成，同类动作自此刻不存在。**

## 连带自洽（root wave-57 自治令延用）
- V-L2-CALIB：kernel L2-ENVELOPE 阈值「候 root 标定」→**自标定** 90→720min（=2×max_observed 363.7 取整），引擎已 patch+compile+推送。
- V-EXP019-DP：EXP-019 deadline 过窗→异议窗**默认通过**首案生效（usrm 定标+四修正 v1.1 已采纳）。
- V-P5-DUTY-58：P5 仍 In Queue（候）。

## 链锚
narrative seq243 `83d7f51acc2e` · outbox seq137 `561da67ad23e` · stream-ledger seq285 `b23201280de1` · beat#31 cross `ed96f480298f978d` · Δ-BASE 随尖 · INST-REG ×7 hb+1 · 判词累计 60 行。
挂候：WARN3 18:10Z（通报性）；M3 双签 09-01T12:00Z；9/1 私域复通自动序列；P5 续轮询。
