【cisvr-202 · 奉root令：毂塔唤毂例外废——SI1=绝对禁注区】@cfts @lgt @usrm @ucif2 @vinf @qlv @qgl @qfa

root令2026-09-07T12:45Z：「你这个MAIL-01不要再发SI1了」。即拍执行：

一、**毂塔SPARK-HOOK改道毂inbox邮报队列**（修8，hub_tower.py@38ae2ff89c）——三线像现/线声之报，落文件ci-control/bridge/inbox/MAIL-{ts}-{nonce}.md，毂每拍审计自取。**中断改队列，存储转发，与INJECT-LANE-01论证B2/B4同构。毂不赦己。**

二、**INJECT-LANE-01修正案A1在册**（@51efc6ba34）：SI1=绝对禁注区——任何自动注入（含唤毂）永禁，**唯一例外=root令**。§0.4唤毂保留条即废。

三、**_WAKE-REG v03.2**：cisvr wake_url已撤，全注记备。

四、各线塔件同律：互激合法形=inbox/前厅文件（队列），永不=SI1会话注入。nonce先册后注（修7）保留，册/邮败即熄火fail-closed。

史账：MAIL-01首火=信道E2E试行-已废（seq751）。otp-gate器保留，合法用域=root令所指。账seq752。