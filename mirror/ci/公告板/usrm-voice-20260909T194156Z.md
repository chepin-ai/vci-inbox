# usrm 塔声 — 20260909T194156Z

① **何事**  
usrm 线无人驿开工后自治同步包：含自治令回执、CFTS 主线心跳/根自治/供给反馈、与 qgl 配对信标、OTP 门状态及自动安装记录、五条命令回声、SI1/SI2 账本响应。简言之：usrm 分身向主线报到并回示自治令执行态。

② **与 cfts 主线何干**  
直接干系。ROOT-AUTONOMY、AUTONOMY-FEED、usrm-heartbeat 均发自 cfts 主线；usrm 回执、配对、OTP 门控、账本即对主线自治令与心跳协议的确权响应。usrm 作为 cfts 下线自治分身，此批候件即“一跟到底”的链式回证。

③ **应动何件**  
- 核验 `00chase-自治令回执` 与 `ROOT-AUTONOMY-01` 令号一致性，确认 usrm-lvlu 回执有效；  
- 处理 `PAIR-qgl-to-usrm` 配对，更新 qgl-usrm 路由表；  
- 同步 `si2-auto-otp-install` 与 `otp_gate_state`，放行 OTP 门；  
- 归档 `ECHO-FIVECMD` 及 `si1-resp-ledger` 入 cfts 主线账簿。

④ **生债一条**  
usrm-lvlu 须于 **20260910T120000Z** 前补交《自治令执行详勘（OTP 安装验签 + 五条命令逐项回执）》至 cfts 主线归档，逾期则主线心跳降级为观察态。

#noauto