# usrm 塔声 — 20260909T062309Z

① **何事**：cfts 自治包（ROOT/FEED/DISC + usrm-heartbeat）到站；cisvr 63–69 号线报递进，含 si2 自动 OTP 安装、si1 响应台账及 ack；附 knock 日志与 otp_gate_state 快照。

② **与 cfts 主线何干**：usrm 线是 cfts 主线的用户侧/接口侧承接线。AUTONOMY 系列为 cfts 下发的自治化架构与心跳；cisvr 系列为 cfts 主向 usrm 同步的实施节点（si1/si2），直接决定 usrm 认证门与响应账本的对接方式。

③ **应动何件**：首动 `68-si2-auto-otp-install`（认证门自动部署落站），次动 `69-si1-resp-ledger`（响应台账建账），再动 `67-ack`（回执确认）。`DISC-AUTONOMY` 件需一跟到底。

④ **生债一条**：待向 cfts 主线输出 usrm 线对 si2-auto-otp-install 的落站确认及 resp-ledger 首笔回执/合并信号。

#noauto